# 为预发布 Schema 重构清空一次测试数据

当前安装仅包含可重建的开发测试数据，因此促销目标拆分为三种可选金额规则、以及卡片从单一卡组织改为一个或两个卡组织时，直接更新当前 Schema，并将本地存储身份切换为 `CardPilotPrereleaseV2`，不编写只服务旧测试数据的迁移层，也不主动删除旧测试存储文件。这项决定只适用于当前预发布阶段；任何发布后的 Schema 变更都必须重新评估并保护用户数据。
